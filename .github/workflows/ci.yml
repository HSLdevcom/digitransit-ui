name: CI

on:
  push:
  pull_request:
    branches: [ master ]

jobs:
  build:
    name: test

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js
        uses: actions/setup-node@v1
        with:
          node-version: '10'
      - name: install dependencies
        run: yarn install
      - name: eslint
        run: yarn lint
      - name: unit tests
        run: yarn test-unit
