/* eslint-disable import/no-extraneous-dependencies */
import { expect } from 'chai';
import { describe, it } from 'mocha';
// eslint-disable-next-line no-unused-vars
import suggestionToLocation from '.';

describe('Testing @digitransit-search-util/digitransit-search-util-suggestion-to-location module', () => {
  it('Checking that true is true', () => {
    // const retValue = suggestionToLocation(param1, param2);
    expect(true).to.be.equal(true);
  });
});
