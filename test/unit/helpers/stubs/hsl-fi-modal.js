// Test stub for `@hsl-fi/modal` (aliased in config/vitest.config.js's `app`
// project). Legacy Rollup UMD bundle with react-modal bundled in; same
// default-export interop problem as @hsl-fi/button, and the unit tests never
// assert on its rendered output.
export default () => null;
