// Test stub for `@hsl-fi/container-spinner` (aliased in
// config/vitest.config.js's `app` project). Legacy Rollup UMD bundle; same
// default-export interop problem as @hsl-fi/button.
export default () => null;
