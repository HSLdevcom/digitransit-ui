// Test stub for `@hsl-fi/button` (aliased in config/vitest.config.js's `app`
// project). The real package is a legacy Rollup UMD bundle whose
// `export default` interop Vite doesn't unwrap for a symlinked workspace-style
// dep, so `import Button from '@hsl-fi/button'` would resolve to the module
// object rather than the component.
export default () => null;
