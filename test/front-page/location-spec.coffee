TEST_NAME = "Front page - Location"

describe TEST_NAME, () ->
  it "Should be automatically searched when page loads"
  it "Should be rediscovered when location box is clicked"
  it "Should be removed by clicking cross icon"
  it "Should be possible to enter manually if locationing is disabled"
  it "Should not be possible to manually enter while locationing is in progress"
  
