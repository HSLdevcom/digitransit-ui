.git
.gitignore
.*swp

.github

LICENSE*
Changelog.md
win-launch-scripts/
docs/
/test

/Dockerfile

node_modules
/.yarn
# todo: keep?
# !/.yarn/install-state.gz
!/.yarn/plugins
!/.yarn/releases
!/.yarn/versions
# We copy the cache into the Dockerfile for Yarn to use it during `yarn install`.
# Because we use a multi-stage build, we don't copy it into the final Docker image.
# !/.yarn/cache

_static
/app/__generated__
/app/**/__generated__
